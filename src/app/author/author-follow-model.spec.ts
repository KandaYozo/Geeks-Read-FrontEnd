import { AuthorFollowModel } from './author-follow-model';

describe('AuthorFollowModel', () => {
  it('should create an instance', () => {
    expect(new AuthorFollowModel()).toBeTruthy();
  });
});
